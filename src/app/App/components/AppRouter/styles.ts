import { styled } from '@mui/material';

export const StyledLoader = styled('div')`
  height: 100%;
  width: 100%;
  position: fixed;
  display: flex;
  background: var(--app-brand-50);

  span {
    margin: auto;
  }
`;
